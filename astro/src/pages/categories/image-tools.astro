---
import CategoryPage from "@/components/discovery/CategoryPage.astro";
import CategoryLayout from "@/layouts/CategoryLayout.astro";
import { createCategoryMetadata } from "@/lib/seo";

const title = "Image Tools Online — Free Image Compressor & Color Palette | ToolBite";
const description =
  "Free image tools including image compressor and color palette generator. All run client-side in your browser — no upload to servers, no account needed.";
const canonicalPath = "/categories/image-tools.html";

const metadata = createCategoryMetadata({
  title,
  description,
  canonicalPath,
  keywords: ["image tools", "compress images", "webp", "optimize images"]
});

const schemas = [
  {
    "@context": "https://schema.org",
    "@type": "CollectionPage",
    name: "Image Tools Online — Free Image Compressor & Color Palette | ToolBite",
    url: "https://toolbite.org/categories/image-tools.html",
    description:
      "Free image compressor and color palette generator run locally in your browser so files are not uploaded to ToolBite for routine processing.",
    dateModified: "2026-05-08"
  }
];
---

<CategoryLayout title={title} description={description} canonicalPath={canonicalPath} metadata={metadata} schemas={schemas}>
  <CategoryPage
    slug="image-tools"
    title="Image & design tools"
    heroDescription="Optimize, compress, and repurpose media assets while keeping file processing local and secure in your browser."
  />
</CategoryLayout>
