name: Site Sanity Checks (local tooling)

# Python scripts live in the sibling Toolbite.org/ folder (outside this repo).
# Run locally from the site repo: python3 ../Toolbite.org/scripts/site_sanity_check.py

on:
  workflow_dispatch:

jobs:
  reminder:
    runs-on: ubuntu-latest
    steps:
      - name: Reminder — sanity scripts are not in this repository
        run: |
          echo "Sanity / SEO / link scripts live beside this repo (see README.md)."
          exit 0
