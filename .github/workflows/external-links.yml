name: External Link Checks (local tooling)

# Script: ../Toolbite.org/scripts/check_external_links.py (outside this repo)

on:
  workflow_dispatch:

jobs:
  reminder:
    runs-on: ubuntu-latest
    steps:
      - name: Reminder — external link checker is not in this repository
        run: |
          echo "Run check_external_links.py from your Toolbite.org tooling folder."
          exit 0
